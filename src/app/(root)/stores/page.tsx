import StoreDirectory from "@/features/vendors/components/store-directory";

export const metadata = {
    title: "Stores | Trendora",
    description: "Browse every seller on the Trendora marketplace.",
};

export default function StoresPage() {
    return <StoreDirectory />;
}
