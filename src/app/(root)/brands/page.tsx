import BrandDirectory from "@/features/products/components/browse/brand-directory";

export const metadata = {
    title: "Brands | Trendora",
    description: "Browse every brand with products in stock on Trendora.",
};

export default function BrandsPage() {
    return <BrandDirectory />;
}
