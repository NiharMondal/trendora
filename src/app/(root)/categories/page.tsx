import CategoryDirectory from "@/features/products/components/browse/category-directory";

export const metadata = {
    title: "Categories | Trendora",
    description: "Browse every category with products in stock on Trendora.",
};

export default function CategoriesPage() {
    return <CategoryDirectory />;
}
